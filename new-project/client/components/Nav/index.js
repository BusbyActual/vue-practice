import style from './style.css'

export default {
  render(h) {
    return (
      <md-toolbar>
        <button>Home</button>
        <button>Projects</button>
        <button>About</button>
      </md-toolbar>
    )
  }
}
